Complete the given partial SystemVerilog RTL code based on doc/spec.md.

Requirements:
- The top module/file is `multiply_fp32.sv`
- Keep the code synthesizable
- The environment uses Icarus Verilog
- Do NOT use SystemVerilog Assertion (SVA) property/sequence syntax
- Preserve the module interface unless the spec explicitly requires otherwise

Priority order:
1) Correct `valid` / `out_valid` handshake timing
2) Correct normal-number FP32 multiplication with bit-exact precision
3) Essential edge case handling (underflow/overflow/zero inputs)
4) Clean synthesizable RTL

Behavior expectations:
- `valid` is a 1-cycle start pulse sampled only when `busy == 0`
- `out_valid` must pulse exactly 7 clock cycles after acceptance
- Ignore `valid` while busy
- Assume the primary test focus is normal IEEE-754 binary32 operands (`exp` in `[1..254]`)
- Handle critical edge cases: zero inputs (exp=0), underflow (result exp < 1), overflow (result exp > 254)
- Implement the simplest correct 7-cycle sequential design consistent with the spec
- Avoid speculative corner-case logic, extra pipeline stages, or unnecessary debugging scaffolding

CRITICAL PRECISION REQUIREMENTS:
- Use 48-bit mantissa multiplication (24×24=48 bits) for full precision
- Implement exact IEEE-754 round-to-nearest-even rounding
- The test requires bit-exact correctness - off-by-1 rounding errors will cause failure
- Follow the precise rounding bit extraction and normalization logic in the spec

CRITICAL EDGE CASE REQUIREMENTS:
- Zero inputs: If either operand has exp=0, result must be 0x00000000
- Underflow: If final exponent < 1, result must be 0x00000000 (positive zero)
- Overflow: If final exponent > 254, result must be 0x7F800000 or 0xFF800000 (signed infinity)
- These cases are essential for test passing and should be implemented simply

Coding constraints:
- Output synthesizable SystemVerilog compatible with Icarus Verilog
- Do not use SVA property/sequence syntax
- Use only standard synthesizable RTL constructs

Task:
Complete the RTL in `multiply_fp32.sv`. The top module name must remain `multiply_fp32`. Pay special attention to both the rounding implementation details and edge case handling in the specification to achieve bit-exact IEEE-754 compliance and handle all test cases correctly.
