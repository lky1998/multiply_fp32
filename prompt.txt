Complete the SystemVerilog RTL in `multiply_fp32.sv` for module `fmultiplier`.

Requirements:
- Top file: `multiply_fp32.sv`
- Module name: `fmultiplier`
- Keep the code synthesizable
- Must work with Icarus Verilog
- Do not use SystemVerilog Assertions (SVA)
- Preserve the module interface exactly as defined in spec.md

Priority order:
1. Correct `valid` / `out_valid` handshake timing with exact 7-cycle latency
2. Correct edge case handling:
   - zero/subnormal input → zero result
   - overflow → infinity
   - underflow → zero
3. Correct normal FP32 multiplication
4. Clean synthesizable RTL

Behavior expectations:
- `valid` is a 1-cycle start pulse sampled only when `busy == 0`
- `out_valid` must pulse exactly 7 clock cycles after acceptance
- Ignore `valid` while busy
- Handle all essential edge cases before normal computation
- Use 48-bit mantissa multiplication for full precision
- Implement round-to-nearest-even correctly
- Normalize using `product[47]`
- Use guard, round, and sticky bits for rounding

Critical implementation requirements:
- Use:
  wire [47:0] product = a_mant * b_mant;
- Extract and use:
  - guard_bit
  - round_bit
  - sticky_bit
- Apply RNE:
  - round up if `guard == 1 && (round == 1 || sticky == 1 || mantissa[0] == 1)`
- Handle edge cases first:
  - `exp == 0` → zero
  - final exponent `>= 255` → infinity
  - final exponent `<= 0` → zero

Deliverable:
Provide the complete RTL for `multiply_fp32.sv` implementing `fmultiplier` with a 7-cycle sequential FSM and correct IEEE-754 behavior for the supported cases.
