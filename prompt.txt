Complete the given partial SystemVerilog RTL code based on doc/spec.md.

Requirements:
- The top module/file is `multiply_fp32.sv` 
- The module name must be `fmultiplier` (as specified in doc/spec.md)
- Keep the code synthesizable
- The environment uses Icarus Verilog
- Do NOT use SystemVerilog Assertion (SVA) property/sequence syntax
- Preserve the module interface as defined in the specification

Priority order:
1) Correct `valid` / `out_valid` handshake timing (exactly 7 cycles)
2) Essential edge case handling (zero inputs, overflow, underflow)
3) Correct normal-number FP32 multiplication with bit-exact precision
4) Clean synthesizable RTL

Behavior expectations:
- `valid` is a 1-cycle start pulse sampled only when `busy == 0`
- `out_valid` must pulse exactly 7 clock cycles after acceptance
- Ignore `valid` while busy
- Handle essential edge cases as specified: zero inputs → zero result, overflow → infinity, underflow → zero
- Focus on normal IEEE-754 binary32 operands for the main computation path
- Implement the 7-cycle sequential design as outlined in the FSM stages

CRITICAL IMPLEMENTATION REQUIREMENTS:
- Use 48-bit mantissa multiplication (24×24=48 bits) for full precision: `wire [47:0] product = a_mant * b_mant;`
- Implement exact IEEE-754 round-to-nearest-even rounding
- Handle normalization correctly: check product[47] to determine shift and rounding bit positions
- Extract rounding bits based on normalization: guard, round, sticky bits
- Apply RNE rule: round up if `guard == 1 && (round == 1 || sticky == 1 || mantissa[0] == 1)`
- Handle edge cases FIRST before normal computation:
  * Zero inputs (exp == 0) → return zero with correct sign
  * Overflow (final exp ≥ 255) → return infinity with correct sign  
  * Underflow (final exp ≤ 0) → return zero with correct sign

Coding constraints:
- Output synthesizable SystemVerilog compatible with Icarus Verilog
- Do not use SVA property/sequence syntax
- Use only standard synthesizable RTL constructs
- Follow the 7-stage FSM structure outlined in the specification

Task:
Complete the RTL in `multiply_fp32.sv` with module name `fmultiplier`. The implementation must handle both normal numbers and the essential edge cases specified in doc/spec.md to achieve reliable test results. Pay special attention to the edge case handling and rounding implementation details to achieve bit-exact IEEE-754 compliance.
